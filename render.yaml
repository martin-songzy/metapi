services:
  - type: web
    name: metapi
    runtime: docker
    repo: https://github.com/martin-songzy/metapi
    dockerfilePath: ./docker/Dockerfile
    dockerContext: .
    plan: free
    envVars:
      - key: AUTH_TOKEN
        sync: false
      - key: PROXY_TOKEN
        sync: false
      - key: TZ
        value: Asia/Shanghai
      - key: PORT
        value: "4000"
      - key: DATA_DIR
        value: /app/data
      - key: NODE_ENV
        value: production
      - key: DB_TYPE
        value: postgres
      - key: DB_URL
        sync: false
      - key: DB_SSL
        value: "true"
      # Render's free plan caps outbound bandwidth, and both background passes
      # below talk to the external database even while the service is idle.
      - key: ADMIN_SNAPSHOT_WARM_INTERVAL_MS
        value: "0"
      - key: USAGE_PROJECTION_INTERVAL_MS
        value: "120000"
      # This deployment only uses site management and check-in, so all routing
      # background maintenance is off. Turn this back on before proxying /v1/*.
      - key: PROXY_ROUTING_ENABLED
        value: "false"
      - key: SITE_ANNOUNCEMENT_POLL_INTERVAL_MS
        value: "3600000"
      - key: CHECKIN_CRON
        value: "0 8 * * *"
      - key: BALANCE_REFRESH_CRON
        value: "0 */6 * * *"
